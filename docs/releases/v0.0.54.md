# v0.0.54 — release preparation

Intended next patch release; the release workflow determines the version from published tags. Repository manifests remain at the current source version until that workflow prepares its release commit.

## Changes

- Dictation processing now belongs to one native session. Settings are captured at recording start, and observing a completed result cannot repeat paste.
- Save the original transcript before cleanup and delivery. If saving fails, return the text for copying and skip automatic paste.
- Validate cleanup and dictionary proposals for supported protected details. Keep the original when a check fails.
- Distinguish confirmed insertion, unconfirmed paste and failed delivery in History. Uncertain paste is never retried automatically.
- Include audio finalization and observed insertion in native timing. Serialize clipboard restoration with new publications to prevent stale restore timers from interfering with a subsequent dictation.

## Measured result

Speech error counts are unchanged on the repeated 30-clip comparison. Cleanup blocks the known incorrect rupee amount and a dropped name, but also rejects one valid time-formatting edit: reference matches change from 25/38 to 24/38. Timing variation does not establish a speed improvement. See the [full comparison and evidence](../benchmarks/native-pipeline-2026-09-19.md).

## Acceptance

Local validation passes: 115 Rust unit tests and one logging integration test (four model-dependent tests ignored), 95 Node tests, Swift tests (two passed, one asset-dependent test skipped), 19 public-corpus tests, four supervisor tests, production build, notices/logging checks and dependency audits. Chromium and WebKit pass smoke, transcript details, security, audio history, correction feedback, dictation, recovery and cleanup checks. CI status is tracked in [PR #79](https://github.com/shekhardtu/linty/pull/79).

The signed `0.0.54-rc.2` local bundle passes `codesign --verify --deep --strict`. Developer ID signing succeeded after unlocking the Mac. Its archive SHA-256 is `451cafe89a004f160e3977f9db9b1a7d4e4c63f1e788f14beb3015d668b585a5`; public notarization still belongs to release CI.

**Packaged native acceptance passed for the tested paths.** Unlocking resolved the earlier startup block without an audio-service restart. Live testing found and fixed a main-thread violation in the native terminal panel-hide timer. The rebuilt Finder-launched candidate passed recording, transcription, exact insertion in TextEdit and Chrome, and insertion with an honest unverified outcome in Zed. Rich clipboard restoration, preserving a new copy, and quitting during recording were checked. The original app was restored afterward. See the [native acceptance report](v0.0.54-native-acceptance.md) for evidence and limitations. Publication and validation of the signed/notarized distributable remain separate release steps.

Cleanup remains opt-in. Preservation checks are conservative and incomplete; unverified delivery can still have inserted successfully. Check the destination before manually retrying.
